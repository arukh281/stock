# Copy to sandbox/.env (gitignored). Used by ./run-api.sh, ./database.sh, and the API process.

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

PYTHON=44ma/.venv/bin/python

ANALYZE_API_KEY=dev-secret
API_PORT=8000
USE_NIFTY100=true

# PYTHONPATH is set automatically by run-api.sh / env.sh if omitted
